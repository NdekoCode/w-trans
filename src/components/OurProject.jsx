import React from "react";

const OurProject = () => {
  return (
    <section id="projects" className="px-3 py-20 md:lg:xl:px-40 bg-opacity-10 ">
      <div className="flex items-center justify-between">
        <h2 className="title title-2 max-w-md">Nos derniers projets</h2>
        <a href="#" className="btn btn-primary py-2 px-3">
          Voir tout
        </a>
      </div>
    </section>
  );
};

export default OurProject;
